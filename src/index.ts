import isDue from "./isDue";

export const sum = (a: number, b: number) => {
  // if ("development" === process.env.NODE_ENV) {
  //   console.log("beep");
  // }
  return a + b;
};

export { isDue };
